# 前端配置示例
# 复制本文件为 frontend/.env 后再填写真实值
#   cp frontend/.env.example frontend/.env
# 注意：VITE_* 变量会被打包进浏览器端代码，只能填写可公开的前端配置

# 后端 API 地址（开发环境可留空，默认使用 Vite 代理）
VITE_API_BASE_URL=

# ── 高德地图 Key（必填！否则地图/导览功能不可用）──────────────────
# 申请地址：https://console.amap.com/dev/key/app
# 需要创建「Web端(JS API)」类型的 Key，用于旅行导览页的 GPS 定位和地图交互
VITE_AMAP_WEB_JS_KEY=请填写你的高德 Web JS Key

# 高德地图 Web Key（可选，用于首页静态地图预览）
VITE_AMAP_WEB_KEY=请填写你的高德 Web Key
